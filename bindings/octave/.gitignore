# Octave MEX binding build artifact (compiled from bindings/octave/mex/ptiff_octave.cpp
# via `make -C bindings/octave/mex build`). The .m wrappers + C++ source in mex/
# are versioned; the .oct shared module is regenerated per-platform.
mex/ptiff_octave.oct
