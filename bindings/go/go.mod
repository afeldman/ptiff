// Module for the Go bindings to libptiff.
//
// The module lives under bindings/go and is consumed as a local module by
// downstream projects (replace directives) until it is published.
module github.com/crater/ptiff-go

go 1.26.5
