# Generated by `make -C ../swig go` (SWIG output, promoted here -- see the
# "alles in SWIG" migration notes in bindings/swig/README.md). Never commit:
# regenerated by the bindings/go/CMakeLists.txt ctest step, or manually via
# `make -C ../swig go`.
ptiff/ptiff.go
ptiff/ptiff_wrap.c
ptiff/ptiff_wrap.h
ptiff/cgo_flags.go
ptiff/roundtrip*.tif
ptiff/*.test

# go module lock file (generated when the module gains dependencies)
go.sum
