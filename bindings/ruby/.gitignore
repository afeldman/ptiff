# Generated by `make -C ../swig ruby` (SWIG output, promoted here -- see the
# "alles in SWIG" migration notes in bindings/swig/README.md). Never commit:
# regenerated via `make -C ../swig ruby`.
lib/ptiff.bundle
lib/ptiff.so
lib/ptiff_wrap.c
test/roundtrip_ruby.tif

# gem build output
*.gem
