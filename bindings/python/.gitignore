# Generated by `make -C ../swig python` (SWIG output, promoted here -- see the
# "alles in SWIG" migration notes in bindings/swig/README.md). Never commit:
# regenerated via `make -C ../swig python`.
src/ptiff.py
src/ptiff_wrap.c
src/_ptiff*.so
src/_ptiff*.pyd
test/roundtrip_python.tif

# build artifacts
dist/
*.egg-info/
