# SWIG binding for the libptiff C ABI (`bindings/swig`)

One SWIG interface file (`ptiff.i`) drives **four** target languages — Python,
Go, Ruby and Octave — from the real C ABI headers in `bindings/c`. The
generated wrappers for Python/Go/Ruby are plain C (no `-c++`), bound only to
the `extern "C"` surface of `libptiff_c`, so they carry **no C++ runtime, no
C++ headers, no libstdc++** (confirmed with `otool -L`: only `libptiff_c`
linkage). Octave differs: the SWIG-Octave runtime (`octave_swig_ref`, the
`octave_value` type system, `mkoctfile`) is C++, so the Octave wrapper is
generated as C++ and compiled with `mkoctfile` — it still binds only the
`extern "C"` surface of `libptiff_c`.

> **Place in the project — DECISION (2026-08-11).** SWIG is the **primary** binding
> surface. The maintainer has chosen *"alles in SWIG"*: the SWIG-generated
> modules **replace** the hand-written bindings (`bindings/go`,
> `bindings/python`, `bindings/ruby`) one language at a time.
> **Go is promoted** (2026-08-12): `bindings/go/ptiff` is now SWIG output,
> regenerated from this directory's `ptiff.i` by `make -C bindings/swig go`
> (see [`bindings/go/README.md`](../go/README.md)); the hand-written cgo
> wrapper is deleted. **Python is promoted** (2026-08-13): `bindings/python/src`
> is now SWIG output, regenerated by `make -C bindings/swig python`; the
> hand-written ctypes wrapper is deleted. **Ruby is promoted** (2026-08-13):
> `bindings/ruby/lib` is now SWIG output, regenerated by `make -C bindings/swig
> ruby`; the hand-written Fiddle wrapper is deleted. **All three SWIG targets
> are now promoted** — `bindings/swig` itself no longer holds any staged
> per-language output.
> **Rust has no SWIG target** — the Rust binding stays on rust-bindgen
> (`bindings/rust`). See ["Planned migration"](#planned-migration) below.

> **Status of this file.** Tracked work for `bindings/swig` is coordinated in the
> `todo` tool / commit messages. This README is living documentation; the
> "Planned migration" section records the agreed direction (not yet executed).

## Targets & layout

| Language | SWIG flag | Output dir | Round-trip test | Status |
|----------|-----------|------------|-----------------|--------|
| Python | `-python` | `../python/src/` (promoted) | `../python/test/test_roundtrip.py` | **official** |
| Go | `-go -cgo` | `../go/ptiff/` (promoted) | `../go/ptiff/roundtrip_test.go` | **official** |
| Ruby | `-ruby` | `../ruby/lib/` (promoted) | `../ruby/test/test_roundtrip.rb` | **official** |
| Octave | `-octave` | `../octave/lib/` (promoted) | `../octave/test/test_roundtrip.m` | **official** |

Go's `make go` target writes directly into the promoted, official
`bindings/go/ptiff/` dir (its Go *module* root, `bindings/go/go.mod`, stays
hand-authored and tracked one level up — SWIG only owns the generated package
contents). Python's `make python` target writes directly into the promoted,
official `bindings/python/src/` dir (flat: `ptiff.py` + `_ptiff.so`, no
package subdirectory, matching what `swig -python` itself emits; packaging —
`pyproject.toml`, Sphinx docs — is hand-authored one level up in
`bindings/python`, tracked separately from the generated module contents).
Ruby's `make ruby` target writes directly into the promoted, official
`bindings/ruby/lib/` dir (Ruby convention: `require "ptiff"` resolves
`ptiff.bundle` off `$LOAD_PATH`/`RUBYLIB`; packaging — `ptiff.gemspec`,
`Rakefile` — is hand-authored one level up in `bindings/ruby`). All
generated/compiled files are gitignored everywhere; only the tests, `ptiff.i`,
`typemaps.i`, `Makefile` and `swig_include/` are versioned.

## Files

| Path | Purpose |
|------|---------|
| `ptiff.i` | SWIG interface: `%include`s the real C ABI headers; includes `typemaps.i`; `%nodefaultctor/dtor` for the opaque handles |
| `typemaps.i` | **Language-guarded** buffer + out-param typemaps (`SWIGPYTHON` / `SWIGGO` / `SWIGRUBY` / `SWIGOCTAVE`) |
| `swig_include/ptiff_c_export.h` | Shim export header (see below) |
| `Makefile` | builds `real_inc/`, runs SWIG per language, compiles, runs all tests against the promoted dirs |

All four languages' round-trip and full test suites now live in their
promoted, official dirs — see [`../go/ptiff/`](../go/ptiff)
([`bindings/go/README.md`](../go/README.md)),
[`../python/test/`](../python/test)
([`bindings/python/README.md`](../python/README.md) / `docs/`),
[`../ruby/test/`](../ruby/test) ([`bindings/ruby/README.md`](../ruby/README.md)),
and [`../octave/test/`](../octave/test)
([`bindings/octave/README.md`](../octave/README.md)).
This directory (`bindings/swig`) holds only the shared `ptiff.i`/`typemaps.i`/
`Makefile`/`swig_include/` driving all four.

## Why the shim include dir (`real_inc/`)

SWIG parses headers with its own C preprocessor, and
`bindings/c/ptiff_c_export.h` expands `PTIFF_C_API` to
`__attribute__((visibility("default")))` on GCC/clang — which SWIG cannot parse.
The build therefore creates **`real_inc/`** as a byte-identical copy of
`bindings/c/` with only `ptiff_c_export.h` swapped for
`swig_include/ptiff_c_export.h`, which defines `#define PTIFF_C_API` (empty).

SWIG parses against `real_inc/` (`-I real_inc`); the C-compiled wrapper still
uses the **real** `bindings/c` headers via the `%{ ... %}` block (emitted
verbatim into the generated wrapper), so struct layouts and prototypes come from
the authoritative source. `real_inc/` is a reproducible build artifact — `make`
regenerates it.

## Why typemaps are language-specific

SWIG typemaps are **not** portable across languages: a Python `Py_buffer`
typemap has no meaning to Go or Ruby. Each language needs its own mapping of the
C ABI's counted `(ptr, size)` buffers and `size_t*`/`int*` out-params. In
`typemaps.i` the four are selected by the SWIG-provided preprocessor macros
(`SWIGPYTHON`, `SWIGGO`, `SWIGRUBY`, `SWIGOCTAVE`), which are always defined for
the matching target:

- **Python** — write buffers collapse onto a readable buffer (`bytes`/`bytearray`/
  `memoryview`); read buffers onto a **writable** one (`PyBUF_WRITABLE`);
  `int *err_out` / `size_t *bytes_read` become extra return values.
- **Go** — `(ptr, size)` pairs collapse onto `[]byte` (mirrors SWIG's own
  `go/cdata.i`: the C wrapper pulls `.array`/`.len` off the Go slice); out-params
  stay in/out pointers (`*int`, `*int64`) matching Go's return-by-pointer idiom.
- **Ruby** — buffers collapse onto a `String` (`RSTRING_PTR`/`RSTRING_LEN`);
  out-params become extra return values (`SWIG_Ruby_AppendOutput`).
- **Octave** — buffers accept a `uint8` (row) array or a `String`; read
  buffers are returned back as an extra `uint8` output value (Octave arrays
  are copy-on-assign, so there is no in-place mutation); out-params become
  extra return values (`SWIG_Octave_AppendOutput`).

So per-language the call surface is:

```python
res = ptiff.ptiff_source_open(OUT)   # -> [handle, err_out]
rc, n = ptiff.ptiff_source_read_tile(src, 1, 0, buf)  # buf: writable bytearray
```

```go
src := Ptiff_source_open(out, &errOut)                 // handle; errOut via ptr
rc := Ptiff_source_read_tile(src, 1, 0, buf, &nread)   // buf: []byte filled in place
```

```ruby
res  = Ptiff::ptiff_source_open(OUT)                   # -> [handle, err_out]
res2 = Ptiff::ptiff_source_read_tile(src, 1, 0, buf)   # -> [rc, bytes_read]; buf: String
```

```octave
[src, ~]   = ptiff_source_open(OUT);                   % handle + int err_out
[rc, d, n] = ptiff_source_read_tile(src, 1, 0, uint8(zeros(1, bs)));  % d: uint8 data
```

## Building and testing

Requires `swig` (≥4.x), a build of `libptiff_c` reachable via **pkg-config** (or
`PTIFF_C_LIB_DIR`), plus the target toolchains (`python3`, `go`, `ruby` and, for
Octave, `octave`/`mkoctfile`).

```bash
# build the shared C ABI once (from the repo root)
cmake -B build -S . -DBUILD_SHARED_LIBS=ON -DPTIFF_BUILD_C_BINDINGS=ON
cmake --build build --target ptiff_c
cmake --install build --prefix /where/ever

# build + run all four round-trip + feature test suites
cd bindings/swig
PKG_CONFIG_PATH=/where/ever/lib/pkgconfig make test
```

Useful targets:

- `make` — build all four (Python extension, Go module, Ruby bundle, Octave module)
- `make python` / `make go` / `make ruby` / `make octave` — build one language
- `make test` — build all, then run all four test suites
- `make clean` — remove generated outputs

`make go` also scaffolds `go/go.mod` (module `github.com/crater/ptiff-bindings-swig`)
and `go/cgo_flags.go` (a `#cgo pkg-config: libptiff_c` directive + rpath) so plain
`go test ./...` in `go/` works without extra flags.

## Status

All four languages round-trip a 32×32 UInt8 image tiled 16×16 (write via
`ptiff_sink_*`, read via `ptiff_source_*`). Known SWIG output notes:

- Go handles are raw value structs wrapping a `uintptr`; close each `sink`/`source`
  **once** (no defer + explicit double-close — it frees the C pointer).
- SWIG Go does not support the `%apply ... OUTPUT` grouping, so `err_out` /
  `bytes_read` stay pointer out-params (idiomatic Go).
- Ruby normalizes `ptiff_version` → `Ptiff_version` etc. (SWIG Warning 801).
- Octave wraps value structs as `new_ptiff_*`/`delete_ptiff_*` pairs and the
  module entry point `ptiff` is a one-shot loader: the test runner calls it
  once and each test guards a second call with
  `exist('ptiff','var')` (see `bindings/octave/README.md`).
- `%nodefaultctor`/`%nodefaultdtor` keep `ptiff_source`/`ptiff_sink` non-constructible.
- SWIG 4.5.0's generated Python shadow-class `__init__` (`SWIG_Python_InitShadowInstance`)
  segfaults on CPython 3.14 (confirmed locally); build against 3.13 or earlier
  (`PYTHON=$(uv run --project ../python python)` picks up the project's pinned
  interpreter — see `.github/workflows/python-bindings.yml`). Not yet reported
  upstream.

## Planned migration — "alles in SWIG"

Agreed direction (with user, 2026-08-11): **SWIG becomes the single, primary
binding layer** for the languages it targets. The hand-written bindings
(`bindings/go`, `bindings/python`, `bindings/ruby`) will be **replaced** by SWIG
outputs and then removed (kept only as reference until the SWIG modules reach
feature/test parity). Work is tracked separately (todo tool / commits); this
section records the scope and acceptance criteria so it can be resumed later.

### Scope

- Replace the **Python, Go, Ruby** bindings with the SWIG-generated modules in
  this directory (promoted to the official language bindings).
- **Rust is out of scope** — there is no SWIG Rust target; `bindings/rust`
  stays on rust-bindgen.
- New languages SWIG supports (Java, C#, Lua, Perl, PHP, R, Tcl, ...) can be
  added cheaply from `ptiff.i` as a side benefit of the C-ABI-first approach,
  but are not part of this first migration.

### Why (rationale)

- One `ptiff.i` + one per-language typemap set replaces three independently
  maintained hand-written wrappers: **one maintenance path** for three
  languages, all covering the full C ABI surface.
- The C ABI (`bindings/c/libptiff_c`) remains the single source of truth.
- SWIG (pure C mode) carries **no C++ runtime** — same guarantee as the
  hand-written C-ABI bindings.

### What must not regress (acceptance criteria)

The hand-written bindings already cover more than the current round-trip tests.
Before replacing/removing them, the SWIG modules must reach **at least parity**
with what they test, so nothing is lost:

- **Go** (`bindings/go/ptiff`): no longer just tile round-trip — version,
  logger, backend registry, error codes, `Image`/`ImageDescriptor`, sink/source
  with proper close-once semantics.
- **Python** (`bindings/python`): version/logger/image/backends features (the
  Cucumber `features/*.feature` suite) plus the ctypes unit tests.
- **Ruby** (`bindings/ruby`): version + tile round-trip tests.

Migration checklist to resume later:

- [x] Port the Go unit-test surface (`version`, `logger`, `backend`, `error`,
      `image`, `sink`) to `go/*_test.go` over SWIG output. 15/15 pass
      (`go/{version,logger,backend,error,image,sink}_test.go`, plus the
      existing `roundtrip_test.go`). Uncovered a real bug in the ported test
      code (not the binding): `Struct_SS_ptiff_sink` is a SWIG Go interface
      wrapping a `uintptr` and is never `== nil` even when the underlying C
      pointer is NULL -- NULL checks must use `sink.Swigcptr() == 0`.
- [x] Port the Python feature suite (or an equivalent set of asserts) over SWIG
      output. 14/14 pass (`python/test_{version,logger,backend,error,image,
      sink}.py`, plus the existing `test_roundtrip.py`). Two follow-on fixes
      needed in `typemaps.i` (SWIGPYTHON block): `ptiff_image_gsd`/
      `ptiff_image_compression`'s bare `double*`/`int*` out-params and
      `ptiff_{runtime,compile_time}_version_out`'s `int* major/minor/patch`
      had no OUTPUT typemap, so pure Python had no way to pass them by
      reference -- added `%apply ... OUTPUT` for `double *out`, `int *out`,
      `int *major`, `int *minor`, `int *patch` (matched by exact param name,
      audited against the rest of the C ABI for collisions). Also found:
      `ptiff_backend_names()`'s returned string is Python-owned (SWIG's
      default `char*` return typemap copies it, same as Go's
      `swigCopyString`), so calling `ptiff_free_string()` on it aborts --
      the original malloc'd buffer is leaked by the generated binding, same
      as the Go port; not fixed (needs a `%newobject`/custom typemap in
      `ptiff.i`, tracked as a follow-up, not a test concern).
      Not yet dropped: ctypes `_ptiff.py`/`ptiff.py` (keep until the
      promotion step below runs).
- [x] Port the Ruby tests over SWIG output. 14/14 pass (`ruby/test_{version,
      logger,backend,error,image,sink}.rb`, plus the existing
      `test_roundtrip.rb`), wired into `make test`. Same two typemap gaps as
      Python existed for Ruby too (SWIGRUBY block was missing the OUTPUT
      typemap for `ptiff_image_gsd`/`ptiff_image_compression`'s bare
      `double*`/`int*` and `ptiff_{runtime,compile_time}_version_out`'s
      `int* major/minor/patch`) -- fixed alongside the Python fix, same
      `%apply` additions, now shared by both language blocks in
      `typemaps.i`. `ptiff_backend_names()` + `ptiff_free_string()` aborts
      the same way as Python, for the same reason (Ruby's default `char*`
      return typemap also copies into a Ruby-owned `String`); not called in
      the Ruby test either. Unlike Go, both Python and Ruby return a plain
      `nil`/`None` for a NULL opaque pointer (no `Swigcptr()`-style
      workaround needed there).
- [x] **Go** promoted (2026-08-12): `make -C bindings/swig go` now writes
      directly into `bindings/go/ptiff/` (its module root, `bindings/go/go.mod`,
      is unaffected -- import path `github.com/crater/ptiff-go/ptiff` did not
      change). The hand-written cgo wrapper (`cgo.go`, `backend.go`, `error.go`,
      `image.go`, `logger.go`, `sink.go`) is deleted; `bindings/go/ptiff/`
      keeps only the ported `*_test.go` files (tracked) plus generated,
      gitignored `ptiff.go`/`ptiff_wrap.c`/`cgo_flags.go`.
      `bindings/go/CMakeLists.txt` now shells out to the SWIG Makefile
      (`ptiff_go_bindings_generate` ctest) instead of hand-rolling `#cgo`
      flags via `file(GENERATE)`. `go-bindings.yml` installs SWIG and runs
      `go vet -unsafeptr=false` (SWIG's generated `ptiff.go` trips vet's
      `unsafe.Pointer` heuristic on its own boilerplate -- a known false
      positive) and `gofmt` over only the hand-written `*_test.go` files
      (generated `ptiff.go` is not gofmt-clean as SWIG emits it).
- [x] **Python** promoted (2026-08-13): `make -C bindings/swig python` now
      writes directly into `bindings/python/src/` (flat `ptiff.py` +
      `_ptiff.so`, no package subdir -- matches raw `swig -python` output).
      The hand-written ctypes wrapper (`src/ptiff/__init__.py`, `_ptiff.py`,
      `ptiff.py`, `py.typed`) and the Cucumber/Gherkin `features/` suite are
      deleted; `bindings/python/test/` keeps only the ported `test_*.py`
      files (tracked) plus generated, gitignored `roundtrip_python.tif`.
      `pyproject.toml` repointed at the flat layout
      (`[tool.hatch.build.targets.wheel.sources] src = ""`,
      `include = ["src/ptiff.py", "src/_ptiff*.so", "src/_ptiff*.pyd"]`);
      `[tool.mypy]` overrides `ptiff` with `follow_imports = "skip"` (SWIG's
      generated import-fallback trips mypy's direct-file-scan even with
      `files` restricted to `test/` -- run bare `mypy`, not `mypy .`, so
      `pyproject.toml`'s `files` config actually applies). 14/14 tests,
      ruff, mypy --strict and the Sphinx build all pass. **Confirmed
      locally: SWIG 4.5.0's generated Python shadow-class `__init__`
      segfaults on CPython 3.14** (`SWIG_Python_InitShadowInstance` /
      `SWIG_Python_GetSwigThis`) -- build against the project's uv-pinned
      3.13 interpreter, not the system `python3` (see the Status section
      above and `.github/workflows/python-bindings.yml`, which already
      builds via `uv run --project bindings/python python`).
- [x] **Ruby** promoted (2026-08-13): `make -C bindings/swig ruby` now writes
      directly into `bindings/ruby/lib/` (Ruby convention: `require "ptiff"`
      resolves `ptiff.bundle` off `$LOAD_PATH`/`RUBYLIB` -- no separate `.rb`
      stub, unlike Python's `ptiff.py`, since SWIG `-ruby` emits only
      `ptiff_wrap.c`/the compiled bundle). The hand-written Fiddle wrapper
      (`lib/ptiff.rb`) is deleted; `lib/ptiff/version.rb` (gem version
      metadata only, `PTiff::VERSION`, independent of the generated bundle)
      survives, same rationale as Go's `go.mod` staying hand-authored one
      level up. `bindings/ruby/test/` keeps only the ported `test_*.rb`
      files (tracked) plus generated, gitignored `roundtrip_ruby.tif`.
      `ptiff.gemspec` repointed at the compiled-extension reality
      (`spec.files` includes `lib/**/*.bundle`; description now states it
      ships a prebuilt native extension, not a pure-Fiddle binder -- no
      `extconf.rb`/native build step, build via `make -C ../swig ruby` for
      the target platform before `gem build`). 14/14 tests pass both via
      `make -C bindings/swig test` (direct `RUBYLIB=`) and via
      `bundle exec rake test` (the gem's own workflow, `Rakefile` puts
      `lib/`+`test/` on the load path). Added `ruby-bindings.yml` CI
      (mirrors `python-bindings.yml`'s shape: build `libptiff_c`, install
      SWIG, `ruby/setup-ruby`, regenerate, `bundle exec rake test` -- Ruby
      had no CI workflow at all before this, hand-written or promoted).
      **All three SWIG targets (Go,
      Python, Ruby) are now promoted** -- `bindings/swig` holds no
      per-language staged output anymore, only the shared `ptiff.i` /
      `typemaps.i` / `Makefile` / `swig_include/`.
- [x] Update `bindings/README.md` diagram + `bindings/c` README references
      now that Go, Python and Ruby are all promoted.
